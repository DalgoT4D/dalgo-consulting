# New Engagement — Artifact Design Plan

This document identifies every artifact that must be designed for the new engagement flow, organized by phase. Each artifact listed here needs a template, schema, or scaffold before the agentic skills can be built around the consulting process.

See [process.md](process.md) for the full workflow reference.

---

## Phase 1: Discovery

| Artifact | Format | What needs to be designed |
|---|---|---|
| Requirements Sheet | Google Sheet | 4-tab workbook — single shared artifact for the entire engagement: **Instructions** (column guide for client), **Engagement Context** (consultant-filled: meeting notes, NGO website URL, program names, overall M&E goals), **Data Sources** (consultant-maintained post-ingestion: raw tables and columns for client reference when writing KPI logic), **Metrics** (client-filled: one row per KPI with metric name, what is measured, calculation logic, data source, audience, frequency, breakdown dimensions, consultant notes). |
| `me_goals.md` | Markdown | LLM-generated summary synthesised from all tabs of the Requirements Sheet (Engagement Context + Data Sources + Metrics). Serves as the single source of truth and shared context for all downstream phases — every subsequent skill (data exploration, framework, model development) reads this file as its primary input. |

---

## Phase 2: Data Exploration

| Artifact | Format | What needs to be designed |
|---|---|---|
| `source.yml` / `sources.yml` (input) | YAML | One or more flexible YAML files that the consultant creates before the LLM runs, or existing dbt source YAMLs the consultant chooses to pass directly. They may include `version`, existing dbt `tests` / `meta` / `tags`, optional `identifier`, partial `columns` blocks, and explicit `pii` flags for consultant-marked sensitive columns. |
| `source.yml` / `sources.yml` (output) | YAML | The same YAML files, enriched in place by the LLM. They preserve user-authored dbt fields, preserve explicit PII markings, add generated profiling fields such as table row counts, candidate primary/join keys, date columns, anomalies, LLM notes, and column-level data types, null rates, cardinality, inferred PII flags, date-format notes, and sample values for columns not marked PII. |

---

## Phase 3: Framework

| Artifact | Format | What needs to be designed |
|---|---|---|
| KPI Framework Sheet | Google Sheet | Multi-tab workbook that acts as the analytics contract for Dalgo outputs. Tabs: **KPI Catalog** (KPI definitions, formulas, sources, grains, mart models, status, change metadata, open questions), **Dashboard Outputs** (dashboard/page/section requirements, audience, cadence, decisions, primary KPIs, filters, drilldowns), **Charts & Visuals** (scorecards, charts, tables, maps, alert lists, chart types, encodings/groupings, visual grain, mart model), **Filters & Drilldowns** (cross-dashboard filters, drilldown paths, source columns, canonical dimensions, defaults), **Alerts** (threshold logic, recipients/audience, cadence, comparison periods, alert grain), **Open Questions** (implementation ambiguities, suggested defaults, owner, status). KPI Framework statuses are **active**, **revised**, **deprecated**, and **needs_client_input**. |
| `kpi_framework.md` | Markdown | Local readable export of the KPI Framework Sheet generated by `read_kpi_framework`. Used by Claude commands for review and context. |
| `kpi_framework.json` | JSON | Local machine-readable export of the KPI Framework Sheet generated by `read_kpi_framework`. Used by `/dbt_plan`, `/generate_er_diagram`, model-development commands, and dbt wizard orchestration. |
| `dbt_plan.md` | Markdown | Output format of `/dbt_plan` — an implementation plan that translates `me_goals.md`, the KPI Framework, and enriched source YAMLs into a dbt architecture. The template must cover required dashboards/metrics/charts/alerts/KPIs; staging, intermediate, and mart model inventories; model dependencies and read paths; per-model joins, aggregations, calculations, and grain; data quality concerns such as duplicates and bad date formats; a cross-dashboard filter and drilldown plan with reusable filterable columns; alert support; validation checks; implementation order; and macro candidates for repeated logic such as date validation, fiscal period derivation, and common KPI calculations. |
| `er_diagram.md` | Markdown (Mermaid) | ER diagram in Mermaid — entities with grain, relationships with cardinality, raw-table-to-entity mapping, and join paths per KPI. Must use `dbt_plan.md` alongside `me_goals.md` and `sources.yml` so entity boundaries match the planned dbt model architecture. |

---

## Phase 4: Model Development

| Artifact | Format | What needs to be designed |
|---|---|---|
| `stg_*.sql` templates | SQL | Staging model scaffold — column rename conventions, cast patterns, dedup logic, null handling, and JSON unnesting patterns. |
| `int_*.sql` templates | SQL | Intermediate model scaffold — join patterns, cohort construction, derived field conventions. |
| `marts_*.sql` templates | SQL | Chart-ready mart model scaffold — aggregation patterns, period logic, filter/drilldown columns, alert-support fields, and long/wide output shapes aligned to the KPI Framework. Only create separate reusable lookup models when the charting contract explicitly needs them. |
| `schema.yml` / dbt `.yml` files | YAML | Structure for AI-authored dbt model and column descriptions, tests (not null, unique, accepted values, relationships), KPI/visual usage notes, and PII notes. Commands must write these YAML files directly, not only describe what should go in them. |
| Data Dictionary Sheet | Google Sheet | Multi-tab client-facing workbook with **Data Dictionary**, **Model Summary**, and **Open Questions** tabs. Descriptions must be plain English and must not include raw PII examples. |
| `data_dictionary.md` | Markdown | Local readable export of the Data Dictionary Sheet for review and handoff. |
| `data_dictionary.json` | JSON | Local machine-readable export of the Data Dictionary Sheet for dbt wizard and downstream automation. |
| `phase4_metadata.json` | JSON | Local state file recording generated models, validation commands/results, Data Dictionary Sheet ID, and finalization status. |

---

## Modification & Investigation Track

| Artifact | Format | What needs to be designed |
|---|---|---|
| `change_request.md` | Markdown | Durable record of the user request, classification, engagement, dbt repo, timestamp, and whether KPI Framework changes are needed. |
| `kpi_framework_patch.json` | JSON | Proposed KPI Framework changes for `/modify`: rows to add, revise, deprecate, affected tabs, exact field changes, status updates, change reason, and change request ID. Written before the confirmation gate and applied only after approval. |
| `dbt_edit_plan.md` | Markdown | Scoped plan for dbt edits: files to edit, files not to touch, layer impact, planned SQL/YAML/macro changes, architecture updates, Data Dictionary impact, validation commands, risks, and implementation checklist. |
| `modification_metadata.json` | JSON | Status and audit metadata for a modification: plan path, confirmation state, files edited, commands run, Data Dictionary refresh status, and whether `dbt_plan.md` / `er_diagram.md` changed. |
| `investigation.md` | Markdown | Diagnostic report for wrong numbers, duplicates, freshness problems, or dashboard discrepancies. Includes issue, context, queries run, findings, likely cause, suggested fix, confidence, inconclusive results, and next step. |
| `queries.sql` | SQL | Reproducible SQL log for every warehouse query executed during `/investigate`, with comments explaining query purpose and no raw PII values in comments. |
| `investigation_metadata.json` | JSON | Status and audit metadata for an investigation: cause_found/inconclusive/blocked, affected KPIs/models, output paths, and recommended next step. |

---

## Summary

| Phase | Artifacts to design |
|---|---|
| 1 — Discovery | Requirements Sheet (GSheet — 4 tabs), `me_goals.md` |
| 2 — Data Exploration | Flexible one-or-more `source.yml` / `sources.yml` input schema, enriched in-place output schema |
| 3 — Framework | KPI Framework Sheet (GSheet), `kpi_framework.md`, `kpi_framework.json`, `dbt_plan.md`, `er_diagram.md` |
| 4 — Model Development | `stg_*.sql`, `int_*.sql`, `marts_*.sql`, `schema.yml` / dbt `.yml` files, Data Dictionary Sheet (GSheet), `data_dictionary.md`, `data_dictionary.json`, `phase4_metadata.json` |
| Modification & Investigation | `change_request.md`, `kpi_framework_patch.json`, `dbt_edit_plan.md`, `modification_metadata.json`, `investigation.md`, `queries.sql`, `investigation_metadata.json` |

**Total: 3 Google Sheet templates, 8 Markdown schemas/templates, 3 YAML schemas/templates, 7 JSON schemas/templates, 4 SQL schemas/scaffolds — 25 artifacts.**
